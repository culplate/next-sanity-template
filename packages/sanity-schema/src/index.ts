import { siteSettings } from './documents/siteSettings';
import { homePage } from './documents/homePage';
import { seo } from './objects/seo';
import { cta } from './objects/cta';

export const schemaTypes = [homePage, siteSettings, seo, cta];
